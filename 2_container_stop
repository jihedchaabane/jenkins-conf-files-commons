pipeline {
    agent any
    environment {
        CONTAINER_NAME = 'container-z-springboot-hello'
    }
    stages {

        stage('Stop Container') {
            steps {
                script {
                    // Arrêter tout conteneur existant
                    sh """
                        docker stop ${CONTAINER_NAME} || true
                    """
                }
            }
        }
    }
    post {
//      always {
//      }
        success {
            echo 'Conteneur arrêté avec succès !'
        }
        failure {
            echo 'Echec d\'arrêter le conteneur !'
        }
    }
}